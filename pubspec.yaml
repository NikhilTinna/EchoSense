name: social_media
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get:
  google_fonts:
  shared_preferences:
  jwt_decoder:
  fluttertoast:
  http: ^1.1.0
  image_picker:
  intl:
  scrollable_positioned_list:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/

